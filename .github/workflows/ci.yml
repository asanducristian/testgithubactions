name: Release on main (tag + release)

on:
  push:
    branches: ["main"]

permissions:
  contents: write

concurrency:
  group: release-main
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tag: ${{ steps.version.outputs.tag }}
      tag_exists: ${{ steps.tagcheck.outputs.exists }}

    steps:
      - name: Checkout (full history + tags)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Determine version
        id: version
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if [ -z "$VERSION" ]; then
            echo "ERROR: Could not read version from package.json"
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Check if tag exists
        id: tagcheck
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          git fetch --tags --force
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Tag $TAG already exists."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Create release assets (for current build)
        run: |
          TAG="${{ steps.version.outputs.tag }}"
          tar -czf dist-${TAG}.tar.gz dist/
          sha256sum dist-${TAG}.tar.gz > dist-${TAG}.tar.gz.sha256

      - name: Upload build artifacts (always)
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ github.sha }}
          path: |
            dist-${{ steps.version.outputs.tag }}.tar.gz
            dist-${{ steps.version.outputs.tag }}.tar.gz.sha256

  release_if_new:
    needs: build
    if: needs.build.outputs.tag_exists == 'false'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout (full history + tags)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: build-${{ github.sha }}

      - name: Create and push tag
        run: |
          TAG="${{ needs.build.outputs.tag }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "$TAG" -m "Release $TAG"
          git push origin "$TAG"

      - name: Create GitHub Release and upload assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.build.outputs.tag }}
          name: ${{ needs.build.outputs.tag }}
          generate_release_notes: true
          files: |
            dist-${{ needs.build.outputs.tag }}.tar.gz
            dist-${{ needs.build.outputs.tag }}.tar.gz.sha256

  bump_version_with_approval:
    needs: build
    if: needs.build.outputs.tag_exists == 'true'
    runs-on: ubuntu-latest
    environment: release-bump
    permissions:
      contents: write

    steps:
      - name: Checkout (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump patch version + commit + push
        run: |
          npm version patch --no-git-tag-version

          git add package.json package-lock.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "ERROR: Version bump produced no changes."
            exit 1
          fi

          git commit -m "chore(release): bump version [SKIP CI]"
          git push origin HEAD:main

      - name: Read new version/tag (after bump) and ensure tag does not exist
        id: bumped
        run: |
          VERSION="$(node -p "require('./package.json').version")"
          if [ -z "$VERSION" ]; then
            echo "ERROR: Could not read bumped version from package.json"
            exit 1
          fi

          TAG="v$VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

          git fetch --tags --force
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "ERROR: After bump, tag $TAG already exists. Refusing to release."
            exit 1
          fi

      - name: Install, test, build (bumped version)
        run: |
          npm ci
          npm test
          npm run build

      - name: Create release assets (bumped version)
        run: |
          TAG="${{ steps.bumped.outputs.tag }}"
          tar -czf dist-${TAG}.tar.gz dist/
          sha256sum dist-${TAG}.tar.gz > dist-${TAG}.tar.gz.sha256

      - name: Create and push tag (bumped version)
        run: |
          TAG="${{ steps.bumped.outputs.tag }}"
          git tag -a "$TAG" -m "Release $TAG"
          git push origin "$TAG"

      - name: Create GitHub Release and upload assets (bumped version)
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.bumped.outputs.tag }}
          name: ${{ steps.bumped.outputs.tag }}
          generate_release_notes: true
          files: |
            dist-${{ steps.bumped.outputs.tag }}.tar.gz
            dist-${{ steps.bumped.outputs.tag }}.tar.gz.sha256
